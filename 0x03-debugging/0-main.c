#include "main.h"

/**
 * main - Tests function that prints if integer is positive or negative
 * Return: 0 if the program completed successfully, or a non-zero number
 */

int main(void)
{
	int number;

	number = 0;
	positive_or_negative(number);

	return (0);
}

